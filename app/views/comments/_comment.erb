<div class= "comment - <%= comment.id %> container"
  style="border: 1px solid black; padding: 1em; margin: 1em;">
  <%= comment.task_id %><br />
  <%# <span> Posted <%= time_ago_in_words(comment.created_at) %>
  <% if current_user == comment.user %>
    <div class="button-group float-end">
      <%= button_to "Destroy", [project, task, comment], ckass:"btn btn-danger", method: :delete %>
    </div>
  <% end %>
  <hr />
  <%= comment.content %>
</div>
